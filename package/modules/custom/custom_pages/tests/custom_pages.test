<?php

/**
 * @file 
 * Contains tests for the Custom Pages module.
 */

/**
 * Test case.
 */
class CustomPagesTestCase extends DrupalWebTestCase {

  /**
   * Implements getInfo().
   */
  public static function getInfo() {
    return array(
      'name' => t('Custom Pages tests'),
      'description' => t('TODO: write me.'),
      'group' => t('Custom Pages'),
    );
  }

  /**
   * Implements setUp().
   */
  function setUp() {
    // Call the parent with an array of modules to enable for the test.
    parent::setUp(array('custom_pages'));

    // TODO: perform additional setup tasks here if required.
  }

  /**
   * Test the module's functionality.
   */
  function testTodoChangeThisName() {
    // TODO: write test!
  }

}
